[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "clinexus"
version = "0.1.0"
description = "Enterprise AI-powered Clinical Trial Intelligence Platform"
readme = "README.md"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["."]